# Local default. Replace with MySQL/PostgreSQL URL in deployment.
DATABASE_URL=sqlite:///./ktb_app.db

# The .h5 file is intentionally not committed. Put the exported fine-tuned
# model in KTB_FastAPI/resnet50_food.h5, or point this variable elsewhere.
MODEL_PATH=resnet50_food.h5
